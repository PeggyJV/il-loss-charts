apps:
  - name: "tg-alerts"
    script: "yarn"
    args: "worker:alerts:tg"
    autorestart: false
    cron_restart: "0 * * * *"
    interpreter: "/bin/bash"
    cwd: "/app/il-loss-charts/packages/workers"
    watch: true
  - name: "discord-alerts"
    script: "yarn"
    args: "worker:alerts:discord"
    autorestart: false
    cron_restart: "0 * * * *"
    interpreter: "/bin/bash"
    cwd: "/app/il-loss-charts/packages/workers"
    watch: true
