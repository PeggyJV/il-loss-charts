{
    "name": "root",
    "private": true,
    "scripts": {
        "dev": "npx concurrently \"cd client && yarn start\" \"cd server && yarn dev\"",
        "build": "npx concurrently \"cd client && yarn build\" \"cd server && yarn compile\"",
        "dev-live": "PORT=4000 cd server && yarn start",
        "prod": "cd server && yarn start"
    }
}