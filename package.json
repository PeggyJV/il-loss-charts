{
    "name": "root",
    "private": true,
    "scripts": {
        "dev": "npx concurrently \"cd packages/client && yarn dev\" \"cd packages/server && yarn dev\"",
        "build": "npx concurrently \"cd packages/client && yarn build\" \"cd packages/server && yarn build\"",
        "dev-live": "PORT=4000 cd packages/server && yarn start",
        "prod": "cd packages/server && yarn start"
    },
    "volta": {
        "node": "14.15.4",
        "yarn": "1.22.10"
    },
    "workspaces": [
        "packages/*"
    ],
    "dependencies": {
        "concurrently": "^5.3.0",
        "rimraf": "^3.0.2"
    }
}
