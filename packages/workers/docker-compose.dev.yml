version: "3.1"
services:
  worker_dev:
    image: sommelier_worker_dev
    build: .
    container_name: sommelier_worker_dev
    env_file: ./.env
    environment:
      - REDIS_URL=127.0.0.1
      - REDIS_PORT=6380
      - NODE_ENV=staging